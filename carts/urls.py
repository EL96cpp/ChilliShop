from django.urls import path, re_path
from .views import *

app_name = 'carts'

urlpatterns = [
    # path('cart_add/', LoginUser.as_view(), name='cart_add'),
    # path('cart_change/', RegisterUser.as_view(), name='cart_change'),
    # path('cart_remove/', profile, name='cart_remove')
]